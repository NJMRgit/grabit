.\" SPDX-License-Identifier: AGPL-3.0-or-later
.\" Copyright (C) 2026 creations
.TH GRABIT 1 "2026-09-03" "grabit 0.7.0" "User Commands"
.SH NAME
grabit \- screenshot, recording, OCR, and upload for Wayland compositors
.SH SYNOPSIS
.B grabit
.RI [ options ]
.br
.B grabit
.BR set | get | unset
.RI [ key "] [" value ]
.br
.B grabit
.RB [ \-p ]
.I plugin
.RI [ args ...]
.br
.B grabit
.B sxcu
.RB { add | list | show | remove }
.RI [ arg ]
.br
.B grabit
.B plugin
.RB { install | list | show | update | remove }
.RI [ arg ]
.br
.B grabit
.B help
.RI [ topic ]
.SH DESCRIPTION
.B grabit
captures region screenshots, records the screen, performs OCR, and uploads the
result, with a native freeze + region selector. It uses
.B zwlr_screencopy_manager_v1
when advertised, otherwise
.B ext_image_copy_capture_manager_v1
with
.BR ext_output_image_capture_source_manager_v1 ,
otherwise the
.B org.kde.KWin.ScreenShot2
dbus service on KDE Plasma.
.PP
Supported compositors: Hyprland, sway, niri, river. KDE Plasma: screenshots via
ScreenShot2, recording via zkde_screencast_unstable_v1 (see
.BR NOTES ).
GNOME: recording only, via org.gnome.Mutter.ScreenCast. X11 is not supported.
.PP
With no action flag, grabit performs the configured
.BR default_action ,
except that
.B \-f
and
.BI \-\- service
imply
.B \-\-upload
on their own
(seeded to
.B copy
on first run, which also writes a default config to
.IR ~/.config/grabit/config.toml ).
.SH ACTIONS
.TP
.BR \-c ", " \-\-copy
Copy the screenshot to the clipboard.
.TP
.BR \-u ", " \-\-upload
Upload the screenshot to the default service (the
.B service
config key). On success the resulting URL is copied to the clipboard and
printed to standard output. Uploaded recordings do the same.
.TP
.BI \-\- service
Upload to a specific built-in service
.RB ( zipline ,
.BR nest ,
.BR fakecrime ,
.BR ez ,
.BR guns ,
.BR pixelvault )
or a registered
.B .sxcu
uploader. Only valid with
.BR \-u ,
.BR \-\-record ,
or no action flag; any other action is an error.
.TP
.BR \-o ", " \-\-output ", " \-\-save
Capture, save to disk, and print the path to standard output. The \(dqSaved\(dq
notification and shutter sound are emitted only when standard output is a
terminal, so redirecting the path to a file or pipe is silent by design.
.TP
.B \-\-record
Toggle screen recording (run again to stop). Records a region to MP4/H.264,
or WebM/GIF via
.BR "grabit set recording.format webm|gif" .
Shows a border overlay, an on-screen control bar (start, pause, stop, abort)
and a tray icon. Sending
.B SIGUSR1
to the recording process toggles pause, for compositor keybinds. See the
recording section of OPTIONS.md for backends and pause/segment behaviour.
.TP
.BR \-M ", " \-\-menu
Freeze the screen and show the action bar: a dock bar along the bottom of the
screen in which you pick what to do with the region you are about to select
.RB ( copy,
save, OCR, translate, upload, pin or record), plus an annotate toggle. The
screen freezes at once, so you can drag a region immediately; the highlighted
action runs as soon as the selection is confirmed and grabit exits.
.B Enter
confirms and
.B Esc
cancels; the region can still be moved and resized while the bar is up, and
.B Ctrl+A
selects the whole screen (handy for a full-screen shot). Clicking a chip in the
bar switches the action, and annotation opens the editor on the captured region
before the action runs.
Doing one thing per keybind still works (see
.BR \-c ", " \-u ", " \-o ", " \-\-record ", " \-\-tesseract );
the bar is for when you want to decide after the region is picked. Wire it to a
key or a launcher entry; the package ships
.BR grabit\-menu.desktop
for exactly that.
.TP
.B \-\-pin
Capture a region and pin it to the desktop. Each pin holds one layer surface per
output, sized to the part of the pin on that output (a drag grows it to the whole
output and moves only the image inside it, because kwin leaves the pixels a
moved layer surface occupied behind), so compositor backdrop
effects such as kwin's blur stay behind the pin instead of tinting the whole
screen. A pin takes the pointer over its own area: hover it for the close button
in its top-right corner, drag its body to move it. Pins stack and persist as
long-lived processes.
.TP
.BR \-\-grab ", " \-\-release ", " \-\-close\-all
Dismiss every pin. Pins are interactive on their own, so
.B \-\-grab
and
.B \-\-release
are no-ops kept for existing compositor hold-binds.
.TP
.B \-\-tray
Toggle a persistent tray icon that runs as a background process. Left click
takes a region screenshot; the context menu offers every action (screenshot,
record, OCR, pin) as submenus. Run again to stop it. The recording tray icon
is separate and appears only while recording (see
.BR \-\-no\-tray " and " recording.tray ).
.TP
.B \-\-tesseract
Capture a region, OCR it via
.BR tesseract ,
and copy the text to the clipboard. Requires
.B tesseract
on
.B $PATH
plus the traineddata for the language being read
.RB ( eng
by default). When
.B ocr.tesseract
is unset, grabit probes
.B tesseract\-ocr
before
.B tesseract
on
.BR $PATH .
Set
.B ocr.lang
to OCR another language, combining several with
.RB \(dq + \(dq
.RB ( "grabit set ocr.lang eng+deu" ).
.TP
.BI \-\-translate [=lang]
With
.BR \-\-tesseract :
translate the OCR text and copy the translation instead.
.I lang
is the target language (default
.BR en ;
source auto-detected). Falls back to raw OCR if the call fails (20s cap).
.B translate.backend
picks the translator:
.BR trans " (default), " libretranslate ", or " deepl .
See
.B OPTIONS.md
for backend setup and DeepL target codes.
.PP
Translating sends the OCR'd text to a third-party server unless you use a
self-hosted LibreTranslate instance.
.TP
.B \-\-show
With
.BR \-\-tesseract :
also render the OCR/translate result on screen as a transient click-through
text card. The text is still copied to the clipboard; add
.B \-\-no\-copy
to show without copying. Auto-dismisses after
.B text_card.dismiss_secs
(default 8s).
.TP
.B \-\-no\-copy
With
.BR \-\-tesseract " " \-\-show :
do not copy the text, only show it.
.TP
.BR \-e ", " \-\-edit
Open the in-tree annotation editor before the action. Pairs with
.BR \-c ,
.BR \-u ,
.BR \-o ,
.B \-\-pin
and
.BR \-\-tesseract ;
ignored for
.BR \-\-record ,
.B \-\-tray
and the pin-management flags.
Make it the default with
.BR "grabit set edit.default true" .
A toolbar opens at the top of the primary monitor (override with
.BR edit.toolbar_output );
drag it by its background to park it anywhere, including onto another
monitor unless
.B edit.toolbar_output
pins it to one; the parked spot is remembered across runs
.RB ( edit.toolbar_pos )
as long as that output is still connected. Setting
.B edit.toolbar_placement
to
.B attach
leaves the toolbar hidden until a region exists, then places it against the
selection instead of at the top, and ignores the parked spot. It hides again
while you drag, move, or resize the region, and settles at the new spot on
release. Keys still work while it is hidden. The editor starts in
region-select mode, but picking any tool switches to drawing immediately, so
the frozen screen can be annotated before a region exists; the select-region
button (or key
.BR q )
drags out (or replaces) the capture area, and Save stays disabled
until one is set. Tools: pen, marker, line, rect, rounded rect, ellipse, arrow,
freehand arrow, blur, pixelate, spotlight, text, counter, callout, eraser (keys
.BR 0\-9 ,
the keypad equivalents, or letters
.BR p / m / l / r / o / a / b / x / h / t / c / k / e ).
Four buttons hold a group of tools, and pressing that group's own key again
cycles it:
.B p
steps pen, marker, line;
.B r
steps rect, rounded rect, ellipse;
.B a
steps arrow, freehand arrow;
.B b
steps blur, pixelate, spotlight. Clicking a group button opens a list of its
tools instead, which together with cycling is the only route to rounded rect
and freehand arrow, the two tools with no key of their own. That list also
offers the line style
.RB ( edit.line_style :
solid, dashed or dotted) for the pen and shape groups.
The freehand arrow draws like the pen but always finishes with an arrow head on
the end of the stroke, so it can curve around whatever it points at.
The spotlight tool dims everything outside the rectangle you drag, with the
width slider setting how dark the surround goes.
The callout tool draws a speech bubble: click what it should point at, type the
text, and the bubble appears offset from that spot with a tail leading back to
it; in the move/resize tool both the bubble and the tail tip are draggable.
The six color buttons can be repointed at any colors with
.B edit.swatches
(a comma separated list of six
.IR #rrggbb ,
.I #rgb
or named colors), or one at a time with
.BR "grabit set edit.swatches " <n> " " <color> ,
where
.I <color>
may be
.B default
to restore that one.
Clicking a swatch that is already selected opens the color
picker on that swatch and repoints it in place, with a reset button beside the
eyedropper to restore that one to its built-in color; either way the row is
remembered across runs.
The move/resize tool
.RB ( s )
repositions drawn annotations: shapes and redactions get four corner handles,
lines, arrows and callouts get two, and everything else moves only. Ctrl+click
adds another annotation to the selection
.RB ( edit.multi_select ),
and
.B Delete
removes the selected ones. There is also an HSL color picker with an eyedropper
and a width slider (or the scroll wheel, which sizes the font instead for text,
counter and callout).
The six color buttons can be repointed with
.B edit.swatches
(six comma separated
.IR #rrggbb ,
.I #rgb
or named colors), or one at a time with
.BR "grabit set edit.swatches " <n> " " <color> ,
where
.I <color>
may be
.B default
to restore that one. Clicking a swatch that is already selected opens the color
picker on it, with a reset button beside the eyedropper.
Undo
.RB ( u
or
.BR Ctrl+Z ,
hold to repeat) steps back through annotations, annotation moves/resizes,
and region changes alike. Redo is
.B Ctrl+y
or
.BR Ctrl+Shift+z .
Last-used color, width, tool and swatch row persist across runs.
.SH NOTES
.SS Touchscreen
The region selector, the recording control bar and pinned captures all accept
touch as well as pointer input: a tap acts as a left click and a drag works the
same as dragging with a mouse. Only one finger is tracked at a time, and there
is no cursor to hover with, so tooltips and hover highlights only appear under
a real pointer.
.SS HDR
On an output using the PQ or HLG transfer function, grabit reads the
colorimetry the compositor publishes over
.B color\-management\-v1
and tone maps the capture to sRGB, so a screenshot looks the way the screen
does. Diffuse white as defined by ITU\-R BT.2408 (203 nits) becomes
sRGB white, and anything brighter is rolled off and clipped.
.PP
Set
.B capture.hdr
to
.B true
to keep the full range instead. A plain
.BR \-F ,
.BR \-w ,
or
.BR \-L
capture to PNG then keeps its 10 bits and is written as a 16\-bit file with the
.BR cICP ,
.BR mDCV ,
and
.B cLLI
chunks describing it. Every other path still tone maps, because the editor,
previews, JPEG and WebP all go through an 8\-bit pipeline. Without
.B color\-management\-v1
grabit cannot know the transfer function and falls back to dropping the extra
bits, which looks flat.
.SS KDE Plasma recording
KWin keeps
.B zkde_screencast_unstable_v1
on an interface blacklist and only exposes it to programs whose installed
.I .desktop
file declares
.BR X-KDE-Wayland-Interfaces=zkde_screencast_unstable_v1 .
grabit ships that key, so recording on Plasma requires an installed grabit
.RB ( make
.BR install ),
not one run from a build tree. Starting KWin with
.B KWIN_WAYLAND_NO_PERMISSION_CHECKS=1
bypasses the check for testing.
.SH MODIFIERS
.TP
.BR \-F ", " \-\-fullscreen
Capture a whole monitor instead of dragging a region. With one monitor it is
grabbed directly. With several, the selector opens dimmed and snaps to whole
monitors: hover a monitor to highlight it and click to grab it (dragging
still works for a custom region). Pairs with any capture action, including
.BR \-\-record ,
and with
.BR \-e ;
cannot be combined with
.BR \-f .
.TP
.BI \-\-fullscreen= monitor
As above, but capture a specific monitor directly (no picker).
.I monitor
is a 1-based number or an output name (e.g.
.BR \-\-fullscreen=DP\-1 ).
Use
.B \-\-fullscreen=all
to capture every monitor stitched into one image. An unknown value lists the
available monitors and exits.
.TP
.BR \-w ", " \-\-window
Capture the focused window instead of dragging a region. Pairs with the same
actions as
.BR \-F ;
cannot be combined with
.BR \-f ,
.BR \-F ,
or
.BR \-\-record .
On Hyprland and sway the window's geometry is read over IPC and the screen is
cropped to it, so
.B \-e
works and anything overlapping the window is included. On sway the rectangle
covers the window and its border but not its title bar, matching what sway's own
IPC reports. On niri, geometry is only
published for floating windows; a floating window takes the same crop path,
while a tiled window is rendered by niri itself via its
.B screenshot\-window
action. That path needs niri 25.11 or newer, captures only the window's own
contents, cannot be annotated (grabit warns and captures without
.BR \-e ),
ignores
.B png.level
for PNG output because niri's own file is used as-is,
and, because niri always copies its window screenshots to the clipboard,
replaces the clipboard contents as a side effect. Other compositors cannot
report the active window and
.B \-w
fails there.
.TP
.BR \-L ", " \-\-last
Reuse the region from the previous capture instead of opening the selector.
The rect is recorded after every region capture and kept in the state file as
.BR region.last .
Works for screenshots and
.BR \-\-record .
With
.BR \-e
the region is applied and locked, so the editor opens on the last-used tool
rather than in region-select mode. Set
.B region.repeat_last
to
.B true
to make this the default without passing the flag.
.B \-F
takes precedence, and the flag is ignored when no region has been recorded yet.
.TP
.BI \-\-delay " secs"
Wait
.I secs
seconds before capturing, so a menu or tooltip can be opened first (0\-3600).
For screenshots the wait happens before the screen is frozen, so anything
opened during it is captured; for
.B \-\-record
it happens after the region is picked, immediately before recording starts.
Set
.B capture.delay
for a default without passing the flag.
.TP
.B \-\-no\-last
Force the region selector for this run even when
.B region.repeat_last
is
.BR true .
Ignored if
.B \-L
is also given.
.TP
.B \-\-cursor
Include the mouse pointer in the screenshot for this run even if
.B capture.cursor
is
.BR false .
The pointer is included by default; hide it with
.BR "grabit set capture.cursor false" .
.TP
.B \-\-chunked
Upload to Zipline in chunks via its partial-upload API (useful behind
Cloudflare's upload size limit). Make it permanent with
.B "grabit set services.zipline.chunked true"
and tune the chunk size (MiB) with
.BR services.zipline.chunk_size .
Plain uploads rejected with HTTP 413 (e.g. Cloudflare's 100MB cap) retry in
chunks automatically, halving the chunk size if needed. Zipline only; other
services ignore it.
.TP
.BI \-f " file"
Use an existing
.I file
instead of capturing.
.TP
.BI \-\-filename " tpl"
Per-run filename template (overrides the
.B filename
config key). See
.BR "FILENAME TEMPLATES" .
.TP
.BI \-\-format " fmt"
Per-run output format:
.BR png ,
.BR jpeg ,
or
.B webp
(default
.BR png ).
JPEG/WebP must have been compiled in.
.TP
.B \-\-no\-upload
With
.BR \-\-record :
skip the auto-upload after recording even if
.BR default_action=upload .
.TP
.B \-\-no\-tray
Suppress the SNI tray icon during recording for one run. Set
.B recording.tray
to
.B false
to disable it permanently.
.TP
.BR \-\-silent ", " \-q ", " \-\-quiet
Suppress info logging, desktop notifications, and the shutter sound (errors
still print). Failure notifications are forced and still appear.
.TP
.BR \-d ", " \-\-debug
Enable debug logging to standard error (same as
.BR GRABIT_DEBUG=1 ).
.TP
.B \-\-
End of options; the following argument is treated as
.BI \-f " file" .
.TP
.BR \-V ", " \-\-version
Print version and exit.
.TP
.BR \-h ", " \-\-help
Print help and exit.
.SH SUBCOMMANDS
.SS Configuration
Every subcommand below also accepts
.B \-\-help
.RB ( \-h )
directly, equivalent to
.BR "grabit help " subcommand .
.TP
.BI "set " "key value"
Write a config key (schema-validated). The
.BI "set " key = value
form is accepted too.
.TP
.BI "set " "edit.swatches n color"
Set one of the six editor color swatches
.RI ( n
is 1-6);
.I color
may be
.B default
to restore that one.
.TP
.BI "set " "key \-\-watch"
Bind a
.BR keys.* \ action
by pressing the key or button. Aliased
.BR \-w .
.TP
.BI "set " "key \-\-reset"
Restore a
.BR keys.* \ default.
.B "set keys \-\-reset"
restores every binding, and
.B "set keys"
lists them all.
.TP
.BI "set " key
Print the example/default value for that key.
.TP
.B set
List all settable keys.
.TP
.BI "get " [key]
Print one config key, or dump every set key.
.TP
.BI "unset " key
Remove a config key.
.TP
.BI "help " [topic]
Print help for a topic:
.BR set ,
.BR get ,
.BR unset ,
.BR sxcu ,
.BR plugin ,
.BR filename ,
.BR env ,
.BR ocr
(aliased
.BR tesseract )
or
.BR examples .
.B grabit \-\-help
.I topic
works too.
.SS Custom uploaders (ShareX .sxcu)
.TP
.BI "sxcu add " "file " [ \-\-force ]
Parse, sanitize, and register a
.B .sxcu
uploader; use it as
.BI \-\- name
(alias:
.BR install ).
.TP
.B sxcu list
Show registered uploaders (alias:
.BR ls ).
.TP
.BI "sxcu show " "name " [ \-\-show\-secrets ]
Print parsed fields; secrets are redacted unless
.B \-\-show\-secrets
is given.
.TP
.BI "sxcu remove " name
Remove an uploader (alias:
.BR rm ).
.SS Plugins
.TP
.BI "plugin install " git\-url
Clone, build, and install a plugin (alias:
.BR add ).
.TP
.B plugin list
List installed plugins (alias:
.BR ls ).
.TP
.BI "plugin show " name
Print the parsed manifest.
.TP
.BI "plugin update " [name]
Update one plugin, or all of them.
.TP
.BI "plugin remove " name
Uninstall a plugin (alias:
.BR rm ).
.TP
.BI name " ..."
Run the installed plugin
.BI grabit\- name
(auto-updates in the background per its manifest). With
.BI \-p " name"
the plugin's last stdout line is pinned to the desktop as a file.
Plugins with
.B capture.auto
receive a fresh screenshot path as their first argument;
.B \-\-capture
and
.B \-\-no\-capture
override that per call. See
.B PLUGINS.md
for the manifest format.
.SH FILENAME TEMPLATES
Tokens accepted by
.B \-\-filename
and the
.B filename
config key:
.TP
.B %Y %m %d %H %M %S
strftime date/time fields.
.TP
.B %s
Unix timestamp.
.TP
.BR %r [ N ]
Random alphanumeric,
.I N
characters (default 12).
.TP
.B %u
UUID v4.
.TP
.B %w
Active window class / app ID.
.TP
.B %t
Active window title.
.PP
.RB ( %w
and
.B %t
read the focused toplevel over
.BR wlr\-foreign\-toplevel\-management\-v1 ,
so they work on Hyprland, sway, niri, and river, and resolve to empty where
that protocol is missing.)
.TP
.B %%
Literal
.BR % .
.PP
Presets via the
.B filename_preset
key:
.B date
.RB ( %Y\-%m\-%d\-%H\-%M\-%S ,
default),
.B random
.RB ( %r12 ),
.B uuid
.RB ( %u ),
.B timestamp
.RB ( %s ).
.SH CONFIGURATION
The config lives at
.B $XDG_CONFIG_HOME/grabit/config.toml
(else
.IR ~/.config/grabit/config.toml ),
written with mode 0600. Manage it with
.B grabit set/get/unset
or edit the TOML directly. Per-service auth tokens go under
.BI services. name .auth
or, preferred, the
.BI GRABIT_ SERVICE _AUTH
environment variable.
.PP
See
.B OPTIONS.md
for the full key reference.
.SH ENVIRONMENT
.TP
.B GRABIT_DEBUG
If set to
.BR 1 ,
enable debug logging (same as
.BR \-d ).
.TP
.BI GRABIT_ SERVICE _AUTH
Per-service auth token, overriding config.
.I SERVICE
is one of
.BR ZIPLINE ,
.BR NEST ,
.BR FAKECRIME ,
.BR EZ ,
.BR GUNS ,
.BR PIXELVAULT .
.TP
.B GRABIT_BIN
Set by plugin dispatch; absolute path to the grabit binary.
.TP
.BR GRABIT_PLUGIN_NAME ", " GRABIT_PLUGIN_DIR ", " GRABIT_CACHE_DIR
Also set by plugin dispatch: the plugin's name, install directory, and
per-plugin cache directory. See
.BR PLUGINS.md .
.TP
.B GRABIT_LOG_FILE
Set to
.B 0
to disable the log file and log to stderr only; any other value forces it on.
Takes precedence over the
.B log_file
config key.
.TP
.B GRABIT_RECORD_ENC_DELAY_US
Test hook: artificially delay every encoder write by this many microseconds to
reproduce a slow encoder and exercise the frame-drop path. Not a tuning knob;
setting it makes recording worse.
.TP
.B GRABIT_CAPTURE_BACKEND
Force the capture backend:
.BR auto ,
.BR wlr ,
.BR ext ,
or
.BR kwin .
Takes precedence over the
.B capture.backend
config key.
.TP
.B GRABIT_CLIPBOARD_BACKEND
Force the clipboard protocol:
.BR auto ,
.BR ext ,
or
.BR wlr .
.B auto
prefers
.B ext_data_control_v1
and falls back to the deprecated
.BR zwlr_data_control_manager_v1 .
.TP
.B WAYLAND_DISPLAY
Wayland socket to connect to. Named in the connection-failure message when
grabit cannot reach a compositor.
.TP
.B HOME
Required when
.B XDG_CONFIG_HOME
or
.B XDG_STATE_HOME
is unset: grabit exits with
.RB \(dq "HOME is not set" \(dq
if neither is present. Also backs the
.IR ~/Pictures ,
.IR ~/Videos ,
and
.I ~/.cache
fallbacks.
.TP
.B XDG_STATE_HOME
Base for
.IR grabit/state.toml ,
default
.IR ~/.local/state .
.TP
.B HYPRLAND_INSTANCE_SIGNATURE
Set by Hyprland; with
.B XDG_RUNTIME_DIR
it locates the Hyprland IPC socket that powers window snapping and
.BR \-w / \-\-window .
.TP
.B NIRI_SOCKET
Set by niri; locates the niri IPC socket that powers
.BR \-w / \-\-window
and window snapping.
.TP
.B SWAYSOCK
Set by sway; locates the sway IPC socket that powers
.BR \-w / \-\-window
and window snapping. Sway's IPC does not list layer surfaces, so bars and
panels are absent from window snapping there. Absent elsewhere, all of this
degrades gracefully.
.TP
.BR XCURSOR_THEME ", " XCURSOR_SIZE
Cursor theme and size for the region selector and overlays
.RI ( XCURSOR_SIZE
is clamped to 8..256, default 24).
.TP
.B XDG_RUNTIME_DIR
Holds the recording pid file and per-pin IPC sockets (falls back to
.IR /tmp ).
.TP
.B XDG_PICTURES_DIR
Screenshot save dir. The
.B save_dir
config key takes precedence; otherwise this, else
.IR ~/Pictures .
.TP
.B XDG_VIDEOS_DIR
Recording save directory. The
.B save_dir
config key takes precedence; otherwise this variable is used, falling back to
.IR ~/Videos .
Screenshots use
.B save_dir
else
.IR ~/Pictures .
.TP
.B TESSDATA_PREFIX
Tesseract language-data directory.
.TP
.B GRABIT_TRANSLATE_KEY
API key for the
.B deepl
and
.B libretranslate
backends, overriding the
.B translate.api_key
config key.
.TP
.B NO_COLOR
If set to any value, disable ANSI color in log output.
.SH FILES
.TP
.I ~/.config/grabit/config.toml
User configuration (TOML), mode 0600. Only ever written by
.BR "grabit set" / unset .
It may hold upload tokens under
.BR services. \fIname\fB .auth ,
so use the
.B GRABIT_ \fISERVICE\fB _AUTH
environment variables instead if you track it in version control.
.TP
.I $XDG_STATE_HOME/grabit/state.toml
Machine-local state grabit writes on its own: the last-used
.BR edit.color ", " edit.width ", " edit.tool ,
the editor color swatches
.RB ( edit.swatches ),
the editor toolbar position
.RB ( edit.toolbar_pos )
and the last captured region
.RB ( region.last ,
used by
.BR \-L / \-\-last ).
Falls back to
.IR ~/.local/state/grabit/state.toml .
These override the matching keys in config.toml, which then act only as an
initial value. Set
.B save_state
to
.B false
to stop reading and writing this file entirely.
.TP
.I ~/.config/grabit/uploaders/<name>.sxcu
Registered ShareX uploaders.
.TP
.I $XDG_RUNTIME_DIR/grabit.log
Every message grabit emits, with a timestamp and pid, including info and debug
lines. Falls back to
.IR /tmp/grabit- <uid> /grabit.log .
Notifications that say
.B check the log file
mean this one, since grabit is usually run from a keybind with no terminal
attached. That line is added only while the log file is enabled. It is
truncated once it grows past 1 MiB. On by default; turn it off
with
.B grabit set log_file false
or
.BR GRABIT_LOG_FILE=0 .
.TP
.I $XDG_RUNTIME_DIR/grabit-show.pid
PID file for the on-screen text/preview card.
.TP
.I $XDG_RUNTIME_DIR/grabit_recording.pid
PID file for the active recording (falls back to
.IR /tmp/grabit- <uid> /grabit_recording.pid ).
.TP
.I $XDG_RUNTIME_DIR/grabit/
Temporary captures for the clipboard and upload flows, removed after use
(falls back to
.IR /tmp/grabit- <uid> /grabit/ ;
grabit refuses a world-writable directory rather than using
.I /tmp
itself).
.TP
.I $XDG_RUNTIME_DIR/grabit-tray.pid
Tray process pid file.
.TP
.I ~/.config/grabit/plugins/<name>/
Installed plugins (binaries symlinked from
.IR plugins/.bin/ ;
per-plugin cache under
.IR ~/.cache/grabit/plugins/<name>/ ).
Plugin bookkeeping lives alongside them:
.I plugins/.lock
(install/update lock) and per-plugin
.IR .source ,
.IR .last_check ,
.I .update.log
files.
.SH EXAMPLES
.TP
.B grabit \-c
Region screenshot to the clipboard.
.TP
.B grabit \-u
Upload using the
.B service
config key.
.TP
.B grabit \-\-zipline
Upload to a specific service.
.TP
.B grabit \-o > shot.txt
Save and capture the path.
.TP
.B grabit \-F \-o
Save a whole-monitor screenshot.
.TP
.B grabit \-\-fullscreen=DP\-1 \-c
Copy monitor DP-1 to the clipboard.
.TP
.B grabit \-e \-u
Annotate, then upload.
.TP
.B grabit \-\-record
Start recording (re-run to stop).
.TP
.B grabit \-\-pin
Pin a region to the desktop.
.TP
.B grabit \-f shot.png \-\-zipline
Upload an existing file.
.TP
.B grabit \-\-tesseract \-\-translate=ja
OCR a region and copy the Japanese translation.
.SH EXIT STATUS
.TP
.B 0
Success.
.TP
.B 1
Runtime failure (capture, upload, clipboard, plugin, or subcommand error).
Cancelling an interactive selection also exits
.BR 1 ,
but without an error notification. Cancelling
.B \-\-record
exits
.B 0
and notifies instead.
.TP
.B 2
Command-line parse or usage error.
.SH SEE ALSO
.BR ffmpeg (1),
.BR tesseract (1),
.BR trans (1)
.PP
Project documentation:
.B OPTIONS.md
and
.B PLUGINS.md
in the source tree.
.SH BUGS
Report issues at
.br
https://heliopolis.live/creations/grabit
.SH LICENSE
AGPL-3.0-or-later.
.SH AUTHOR
creations <creations@creations.works>
